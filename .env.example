# Code generated by internal/tools/configgen; DO NOT EDIT.
# Valid local-auth production baseline. Replace placeholder secrets before use.

# Administration
LIBREDASH_BOOTSTRAP_ADMIN_EMAIL=admin@example.com

# Agent
# LIBREDASH_AGENT_API_KEY=replace-with-secret
# LIBREDASH_AGENT_BASE_URL=https://api.openai.com/v1
# LIBREDASH_AGENT_MODEL=gpt-5

# Assets
# LIBREDASH_ASSET_VERSION=

# Authentication
# LIBREDASH_API_TOKEN_ONLY_AUTH=true
# LIBREDASH_AZURE_CALLBACK_URL=https://libredash.example.com/auth/azureadv2/callback
# LIBREDASH_AZURE_CLIENT_ID=
# LIBREDASH_AZURE_CLIENT_SECRET=replace-with-secret
# LIBREDASH_AZURE_TENANT=
# LIBREDASH_DEV_API_TOKEN=dev
# LIBREDASH_DEV_AUTH_BYPASS=false
LIBREDASH_LOCAL_AUTH=true
# LIBREDASH_OIDC_CALLBACK_URL=https://libredash.example.com/auth/oidc/callback
# LIBREDASH_OIDC_CLIENT_ID=
# LIBREDASH_OIDC_CLIENT_SECRET=replace-with-secret
# LIBREDASH_OIDC_ISSUER_URL=https://issuer.example.com
# LIBREDASH_OIDC_PROVIDER_ID=oidc
# LIBREDASH_OIDC_SCOPES=openid profile email
# LIBREDASH_SCIM_BEARER_TOKEN=replace-with-secret

# Client
# LIBREDASH_API_TOKEN=replace-with-secret
# LIBREDASH_CLI_CONFIG=
# LIBREDASH_TARGET=

# Execution
# LIBREDASH_EXEC_JOB_LEASE_TIMEOUT=2m
LIBREDASH_EXEC_MAX_QUEUED_READS=64
# LIBREDASH_EXEC_MAX_QUEUED_WRITES=64
LIBREDASH_EXEC_MAX_RUNNING_READS=4
# LIBREDASH_EXEC_MAX_RUNNING_WRITES=1
LIBREDASH_EXEC_READ_QUEUE_TIMEOUT=30s
LIBREDASH_EXEC_READ_TIMEOUT=2m
LIBREDASH_QUERY_CACHE_MAX_BYTES=67108864
LIBREDASH_QUERY_CACHE_MAX_ENTRIES=256

# Managed Data
LIBREDASH_MANAGED_DATA_BACKEND=local
LIBREDASH_MANAGED_DATA_DIR=/var/lib/libredash/managed-data
# LIBREDASH_MANAGED_DATA_GC_GRACE_PERIOD=24h
# LIBREDASH_MANAGED_DATA_GC_INTERVAL=1h
# LIBREDASH_MANAGED_DATA_MAX_FILES=10000
# LIBREDASH_MANAGED_DATA_MAX_FILE_BYTES=1073741824
# LIBREDASH_MANAGED_DATA_MAX_REVISION_BYTES=10737418240
# LIBREDASH_MANAGED_DATA_MIN_FREE_BYTES=5368709120
# LIBREDASH_MANAGED_DATA_S3_ACCESS_KEY_ID=replace-with-secret
# LIBREDASH_MANAGED_DATA_S3_BUCKET=libredash-managed-data
# LIBREDASH_MANAGED_DATA_S3_ENDPOINT=https://s3.example.com
# LIBREDASH_MANAGED_DATA_S3_PATH_STYLE=false
# LIBREDASH_MANAGED_DATA_S3_PREFIX=managed-data
# LIBREDASH_MANAGED_DATA_S3_REGION=eu-west-1
# LIBREDASH_MANAGED_DATA_S3_SECRET_ACCESS_KEY=replace-with-secret
# LIBREDASH_MANAGED_DATA_S3_SESSION_TOKEN=replace-with-secret
# LIBREDASH_MANAGED_DATA_UPLOAD_SESSION_TTL=24h

# Operations
# LIBREDASH_HEALTHCHECK_URL=http://127.0.0.1:8080/readyz
LIBREDASH_METRICS_BEARER_TOKEN=replace-with-at-least-32-characters

# Security
LIBREDASH_ALLOWED_HOSTS=libredash.example.com
LIBREDASH_COOKIE_SECURE=true
LIBREDASH_CSRF_KEY=replace-with-at-least-32-characters
# LIBREDASH_TOKEN_HASH_KEY=replace-with-secret
LIBREDASH_TRUST_PROXY_HEADERS=false

# Server
# LIBREDASH_ADDR=:8080
LIBREDASH_PRODUCTION=1

# Storage
# LIBREDASH_DUCKDB_DIR=
LIBREDASH_DUCKLAKE_CATALOG_PATH=/var/lib/libredash/ducklake/catalog.sqlite
LIBREDASH_HOME=/var/lib/libredash
